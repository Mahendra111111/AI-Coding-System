---
description: Pointer — full ACS Cursor rule lives in .cursor/rules/
alwaysApply: false
---

# AI Coding System (example pointer)

**Canonical rule (use this):** [`.cursor/rules/ai-coding-system-mcp.mdc`](../.cursor/rules/ai-coding-system-mcp.mdc)

That file ships with the repo so every installer gets the same workflow: **one-command `install.ps1`** (ACS CLI + providers), Von `decide_tools`, Graphify `graphify-out/graph.json` SoT, SEO-first, Reticle, token discipline, Caveman.

To apply on every Cursor workspace on a machine (also done by `install.ps1`):

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```
