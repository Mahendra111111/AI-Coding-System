---
description: Pointer — full ACS Cursor rule lives in .cursor/rules/
alwaysApply: false
---

# AI Coding System (example pointer)

**Canonical rule (use this):** [`.cursor/rules/ai-coding-system-mcp.mdc`](../.cursor/rules/ai-coding-system-mcp.mdc)

That file ships with the repo so every installer gets the same performance workflow (Von `decide_tools`, SEO-first, Reticle, token discipline, Caveman).

To apply on every Cursor workspace on a machine:

```powershell
Copy-Item -Force ".\.cursor\rules\ai-coding-system-mcp.mdc" "$env:USERPROFILE\.cursor\rules\ai-coding-system-mcp.mdc"
```
